object MyObj{

  def main(args:Array[String]) {
	
	val alphabet = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"

	val shift = (scala.io.StdIn.readLine("no of shift: ").toInt + alphabet.size) % alphabet.size

	val getText = scala.io.StdIn.readLine("massage needs to shift: ")

	val outputText = getText.map( (c: Char) => { 

		val x = alphabet.indexOf(c.toUpper)

		if (x == -1){
			c
		}
		else{
			alphabet((x + shift) % alphabet.size)
			} 
	});

	println(outputText);
    
}

}